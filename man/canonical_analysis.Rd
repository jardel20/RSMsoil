% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical_analysis.R
\name{canonical_analysis}
\alias{canonical_analysis}
\title{Canonical Analysis of Response Surface}
\usage{
canonical_analysis(rsm_model)
}
\arguments{
\item{rsm_model}{An object of class "rsm_model" fitted with a second-order model.}
}
\value{
A list of class "canonical_analysis" containing:
\itemize{
\item \code{stationary_point}: Coordinates of the stationary point in coded scale
\item \code{stationary_point_natural}: Coordinates in natural scale (if encoding levels available)
\item \code{predicted_response}: Predicted response at stationary point
\item \code{hessian_matrix}: Hessian matrix (2 × B matrix)
\item \code{eigenvalues}: Eigenvalues of the Hessian matrix
\item \code{eigenvectors}: Eigenvectors (canonical axes)
\item \code{surface_type}: Classification (maximum, minimum, saddle point)
\item \code{canonical_coefficients}: Coefficients in canonical form
\item \code{factors}: Names of factors
}
}
\description{
Performs canonical analysis to characterize the stationary point and
determine the nature of the response surface (maximum, minimum, or saddle point).
}
\details{
The stationary point is found by solving:

\deqn{\mathbf{b} + 2\mathbf{B}\mathbf{x}_s = 0}

Where \eqn{\mathbf{b}} is the linear coefficient vector and \eqn{\mathbf{B}} is the
quadratic coefficient matrix.

The Hessian matrix is \eqn{\mathbf{H} = 2\mathbf{B}}.

Surface classification:
\itemize{
\item All eigenvalues negative: maximum
\item All eigenvalues positive: minimum
\item Mixed signs: saddle point
}
}
\examples{
data <- tibble::tibble(
  Y = c(6.66, 6.30, 6.32, 5.92, 6.09, 6.22, 5.29, 6.67, 5.67),
  P_coded = c(-1, -1, 1, 1, 0, -1.68, 1.68, 0, 0),
  S_coded = c(-1, 1, -1, 1, 0, 0, 0, -1.68, 1.68)
)

model <- fit_second_order(data, response = "Y", factors = c("P_coded", "S_coded"))
canonical <- canonical_analysis(model)

}
