% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{get_optimal_factors}
\alias{get_optimal_factors}
\title{Find Optimal Factor Levels}
\usage{
get_optimal_factors(
  rsm_model,
  objective = "maximize",
  bounds = NULL,
  n_grid = 20
)
}
\arguments{
\item{rsm_model}{An object of class "rsm_model".}

\item{objective}{Character: "maximize" or "minimize". Default is "maximize".}

\item{bounds}{A list with named numeric vectors specifying lower and upper bounds
for each factor in coded scale. If NULL, uses \eqn{[-1.68, 1.68]} for all factors.}

\item{n_grid}{Integer number of grid points per dimension. Default is 20.}
}
\value{
A list containing:
\itemize{
\item \code{optimal_point}: Tibble with optimal factor levels
\item \code{optimal_response}: Predicted response at optimal point
\item \code{grid_search_results}: Tibble with all evaluated points and responses
}
}
\description{
Searches for optimal factor levels that maximize or minimize the response
within specified bounds.
}
\examples{
data <- tibble::tibble(
  Y = c(6.66, 6.30, 6.32, 5.92, 6.09, 6.22, 5.29, 6.67, 5.67),
  P_coded = c(-1, -1, 1, 1, 0, -1.68, 1.68, 0, 0),
  S_coded = c(-1, 1, -1, 1, 0, 0, 0, -1.68, 1.68)
)

model <- fit_second_order(data, response = "Y", factors = c("P_coded", "S_coded"))
optimization <- get_optimal_factors(model, objective = "maximize", n_grid = 15)

}
